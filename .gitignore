# Public repo — never commit credentials.
*service-account*.json
*-credentials.json
*.key
*.pem
.env
.env.*

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local run noise
run.log
*.log
# mca_cache.json is committed (persisted across CI runs so MCA isn't re-resolved daily)

# dashboard preview screenshots (local review only)
dashboard/.preview-*.png
